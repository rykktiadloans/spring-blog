spring:
  application:
    name: auth-service
  devtools:
    restart:
      enabled: true
      additional-paths:
        - .
    livereload:
      enabled: true

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

logging:
  level:
    root: DEBUG

server:
  port: 8083