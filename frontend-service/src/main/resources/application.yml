spring:
  application:
    name: frontend-service
  devtools:
    restart:
      enabled: true
      additional-paths:
        - .
    livereload:
      enabled: true

logging:
  level:
    root: DEBUG

server:
  port: 8082

services:
  api: ${GATEWAY_URL}